# VaxForge — hafif omurga bağımlılıkları (GPU'suz, yerel)
streamlit>=1.58
biopython>=1.83
pandas>=2.1
pyyaml>=6.0
# B-hücre epitopu: BepiPred-3.0 (tools/bepipred-3.0, bp3 + ESM-2)
torch>=2.0
fair-esm>=2.0
plotly>=5.8
tenacity>=8.0
importlib-resources>=5.8
# IFN-γ (HTL/MHC-II) indükleme: IFNepitope2 (ExtraTrees+BLAST, model+blastp gömülü).
# KURULUM: pip install --no-deps ifnepitope2  (katı pandas==1.4/sklearn==1.2.1
# pinlerini ES GEÇ — venv'deki sürümler kullanılır). Modeller sklearn 0.24 ile
# pickle'lanmış; vaxforge/ifngamma.py çalışma anında kurulu sklearn'e köprüler
# (scripts/convert_ifn_models.py referans CLI için kalıcı dönüştürür).
# ifnepitope2  # --no-deps ile elle kurulur
#
# Ağır/harici araçlar (diamond, prodigal, AlphaFold, GROMACS ...) konteyner
# (podman) veya ayrı GPU makinesinde; buraya dahil değildir.
